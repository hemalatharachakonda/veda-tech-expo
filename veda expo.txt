<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Veda Tech Expo 2026 – Registration</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700;900&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
  :root {
    --bg: #030712;
    --surface: #0d1117;
    --card: #111827;
    --border: #1f2937;
    --gold: #d4a843;
    --gold2: #f0c060;
    --amber: #f59e0b;
    --text: #f1f5f9;
    --muted: #94a3b8;
    --accent: #e8c06a;
    --success: #10b981;
    --error: #ef4444;
    --radius: 12px;
  }
 
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
 
  html { scroll-behavior: smooth; }
 
  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Outfit', sans-serif;
    font-size: 16px;
    line-height: 1.6;
    overflow-x: hidden;
  }
 
  /* ── STARFIELD BACKGROUND ── */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      radial-gradient(1px 1px at 10% 15%, rgba(212,168,67,0.5) 0%, transparent 100%),
      radial-gradient(1px 1px at 30% 60%, rgba(255,255,255,0.3) 0%, transparent 100%),
      radial-gradient(1px 1px at 55% 20%, rgba(212,168,67,0.4) 0%, transparent 100%),
      radial-gradient(1px 1px at 75% 80%, rgba(255,255,255,0.25) 0%, transparent 100%),
      radial-gradient(1px 1px at 90% 40%, rgba(212,168,67,0.35) 0%, transparent 100%),
      radial-gradient(1px 1px at 20% 90%, rgba(255,255,255,0.2) 0%, transparent 100%),
      radial-gradient(1px 1px at 65% 50%, rgba(212,168,67,0.3) 0%, transparent 100%),
      radial-gradient(1px 1px at 45% 75%, rgba(255,255,255,0.2) 0%, transparent 100%);
    pointer-events: none;
    z-index: 0;
  }
 
  /* ── NAV ── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.6rem 2rem;
    background: rgba(3,7,18,0.92);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(212,168,67,0.2);
    height: 68px;
  }
 
  .nav-logo {
    font-family: 'Cinzel', serif;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--gold);
    letter-spacing: 0.08em;
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
  }
  .nav-left, .nav-right {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }
  .nav-logo-img {
    height: 44px;
    width: auto;
    object-fit: contain;
    border-radius: 4px;
    filter: brightness(1.1);
  }
  #klLogo { background: transparent; }
  #vedaLogo { background: transparent; }
 
  .nav-links { display: flex; gap: 2rem; list-style: none; }
  .nav-links a {
    color: var(--muted);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    transition: color 0.2s;
  }
  .nav-links a:hover { color: var(--gold); }
 
  .nav-cta {
    background: linear-gradient(135deg, var(--gold), var(--amber));
    color: #000 !important;
    padding: 0.45rem 1.2rem;
    border-radius: 6px;
    font-weight: 600 !important;
  }
  .nav-cta:hover { opacity: 0.9; }
 
  /* ── HERO ── */
  #hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 8rem 2rem 4rem;
    overflow: hidden;
  }
 
  .hero-glow {
    position: absolute;
    width: 600px; height: 600px;
    background: radial-gradient(circle, rgba(212,168,67,0.12) 0%, transparent 70%);
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
    animation: pulse 4s ease-in-out infinite;
  }
 
  @keyframes pulse {
    0%, 100% { opacity: 0.6; transform: translate(-50%, -50%) scale(1); }
    50% { opacity: 1; transform: translate(-50%, -50%) scale(1.08); }
  }
 
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(212,168,67,0.1);
    border: 1px solid rgba(212,168,67,0.3);
    border-radius: 999px;
    padding: 0.35rem 1rem;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--gold);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    margin-bottom: 2rem;
    animation: fadeUp 0.8s ease forwards;
  }
 
  .hero-badge::before {
    content: '';
    width: 6px; height: 6px;
    background: var(--gold);
    border-radius: 50%;
    animation: blink 1.2s ease-in-out infinite;
  }
 
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.2} }
 
  .hero-title {
    font-family: 'Cinzel', serif;
    font-size: clamp(2.8rem, 7vw, 6rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: 0.02em;
    animation: fadeUp 0.9s 0.1s ease forwards;
  }
 
  .hero-title span {
    background: linear-gradient(135deg, var(--gold) 0%, var(--gold2) 50%, var(--amber) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
 
  .hero-sub {
    font-family: 'Cinzel', serif;
    font-size: clamp(1rem, 2.5vw, 1.4rem);
    color: var(--muted);
    letter-spacing: 0.25em;
    text-transform: uppercase;
    margin-top: 0.5rem;
    animation: fadeUp 1s 0.2s ease forwards;
  }
 
  .hero-desc {
    max-width: 580px;
    margin: 1.5rem auto 0;
    color: var(--muted);
    font-size: 1.05rem;
    font-weight: 300;
    animation: fadeUp 1s 0.3s ease forwards;
  }
 
  .hero-divider {
    width: 80px; height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 2rem auto;
    animation: fadeUp 1s 0.35s ease forwards;
  }
 
  .hero-buttons {
    display: flex; gap: 1rem;
    flex-wrap: wrap; justify-content: center;
    animation: fadeUp 1s 0.4s ease forwards;
  }
 
  .btn-primary {
    background: linear-gradient(135deg, var(--gold), var(--amber));
    color: #000;
    font-weight: 600;
    padding: 0.8rem 2.2rem;
    border-radius: 8px;
    text-decoration: none;
    font-size: 0.95rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    transition: all 0.2s;
    box-shadow: 0 0 24px rgba(212,168,67,0.3);
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 0 36px rgba(212,168,67,0.5); }
 
  .btn-secondary {
    background: transparent;
    color: var(--gold);
    border: 1px solid rgba(212,168,67,0.4);
    font-weight: 500;
    padding: 0.8rem 2.2rem;
    border-radius: 8px;
    text-decoration: none;
    font-size: 0.95rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    transition: all 0.2s;
  }
  .btn-secondary:hover { background: rgba(212,168,67,0.08); border-color: var(--gold); }
 
  .hero-stats {
    display: flex; gap: 3rem;
    justify-content: center;
    margin-top: 4rem;
    animation: fadeUp 1s 0.5s ease forwards;
    flex-wrap: wrap;
  }
  .stat { text-align: center; }
  .stat-num {
    font-family: 'Cinzel', serif;
    font-size: 2rem;
    font-weight: 700;
    color: var(--gold);
    display: block;
  }
  .stat-label { font-size: 0.8rem; color: var(--muted); text-transform: uppercase; letter-spacing: 0.08em; }
 
  /* ── ABOUT ── */
  #about {
    padding: 6rem 2rem;
    max-width: 1100px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }
 
  .section-header { text-align: center; margin-bottom: 3.5rem; }
  .section-tag {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--gold);
    margin-bottom: 0.75rem;
  }
  .section-title {
    font-family: 'Cinzel', serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    font-weight: 700;
    line-height: 1.2;
  }
  .section-line {
    width: 60px; height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 1rem auto 0;
  }
 
  .about-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }
 
  .about-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 2rem;
    position: relative;
    overflow: hidden;
    transition: border-color 0.3s, transform 0.3s;
  }
  .about-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    opacity: 0;
    transition: opacity 0.3s;
  }
  .about-card:hover { border-color: rgba(212,168,67,0.3); transform: translateY(-4px); }
  .about-card:hover::before { opacity: 1; }
 
  .card-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
    display: block;
  }
  .card-title {
    font-family: 'Cinzel', serif;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--gold);
    margin-bottom: 0.75rem;
  }
  .card-text { color: var(--muted); font-size: 0.95rem; font-weight: 300; }
 
  /* ── REGISTRATION ── */
  #register {
    padding: 6rem 2rem;
    position: relative;
    z-index: 1;
    background: linear-gradient(180deg, transparent, rgba(212,168,67,0.03), transparent);
  }
 
  .register-wrap {
    max-width: 820px;
    margin: 0 auto;
  }
 
  .form-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 3rem;
    position: relative;
    overflow: hidden;
  }
  .form-card::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--gold), var(--amber), var(--gold));
  }
 
  .form-section {
    margin-bottom: 2.5rem;
  }
  .form-section-title {
    font-family: 'Cinzel', serif;
    font-size: 1rem;
    font-weight: 700;
    color: var(--gold);
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin-bottom: 1.25rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
 
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }
  .form-grid.full { grid-template-columns: 1fr; }
  @media (max-width: 600px) { .form-grid { grid-template-columns: 1fr; } }
 
  .field { display: flex; flex-direction: column; gap: 0.4rem; }
  .field.span2 { grid-column: span 2; }
  @media (max-width: 600px) { .field.span2 { grid-column: span 1; } }
 
  label {
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
  }
  label .req { color: var(--amber); margin-left: 2px; }
  label .opt { color: #4b5563; font-size: 0.7rem; font-weight: 400; margin-left: 4px; }
 
  input, select, textarea {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.7rem 1rem;
    color: var(--text);
    font-family: 'Outfit', sans-serif;
    font-size: 0.95rem;
    width: 100%;
    transition: border-color 0.2s, box-shadow 0.2s;
    outline: none;
  }
  input:focus, select:focus, textarea:focus {
    border-color: rgba(212,168,67,0.5);
    box-shadow: 0 0 0 3px rgba(212,168,67,0.08);
  }
  input::placeholder, textarea::placeholder { color: #374151; }
  select option { background: var(--card); }
  textarea { resize: vertical; min-height: 80px; }
 
  /* member blocks */
  .members-container { display: flex; flex-direction: column; gap: 1rem; }
 
  .member-block {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1.25rem;
    position: relative;
  }
 
  .member-block-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }
  .member-label {
    font-family: 'Cinzel', serif;
    font-size: 0.85rem;
    font-weight: 700;
    color: var(--gold);
    letter-spacing: 0.06em;
  }
  .remove-member {
    background: rgba(239,68,68,0.1);
    border: 1px solid rgba(239,68,68,0.2);
    color: #ef4444;
    padding: 0.25rem 0.6rem;
    border-radius: 6px;
    font-size: 0.75rem;
    cursor: pointer;
    transition: all 0.2s;
    font-family: 'Outfit', sans-serif;
  }
  .remove-member:hover { background: rgba(239,68,68,0.2); }
 
  .add-member-btn {
    background: rgba(212,168,67,0.07);
    border: 1px dashed rgba(212,168,67,0.3);
    color: var(--gold);
    border-radius: 8px;
    padding: 0.75rem;
    font-size: 0.9rem;
    font-family: 'Outfit', sans-serif;
    font-weight: 500;
    cursor: pointer;
    width: 100%;
    margin-top: 0.5rem;
    transition: all 0.2s;
  }
  .add-member-btn:hover { background: rgba(212,168,67,0.12); border-color: var(--gold); }
  .add-member-btn:disabled { opacity: 0.4; cursor: not-allowed; }
 
  /* submit */
  .submit-wrap { margin-top: 2rem; text-align: center; }
 
  .btn-submit {
    background: linear-gradient(135deg, var(--gold), var(--amber));
    color: #000;
    font-weight: 700;
    padding: 1rem 3.5rem;
    border-radius: 10px;
    border: none;
    font-size: 1rem;
    font-family: 'Outfit', sans-serif;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.25s;
    box-shadow: 0 4px 24px rgba(212,168,67,0.25);
    position: relative;
    overflow: hidden;
  }
  .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 8px 32px rgba(212,168,67,0.4); }
  .btn-submit:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }
 
  /* ── SUCCESS MODAL ── */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(3,7,18,0.9);
    backdrop-filter: blur(8px);
    z-index: 200;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s;
  }
  .modal-overlay.show { opacity: 1; pointer-events: auto; }
 
  .modal {
    background: var(--card);
    border: 1px solid rgba(212,168,67,0.3);
    border-radius: 16px;
    padding: 3rem 2rem;
    max-width: 440px;
    width: 100%;
    text-align: center;
    position: relative;
    transform: scale(0.9);
    transition: transform 0.3s;
  }
  .modal-overlay.show .modal { transform: scale(1); }
 
  .modal-icon {
    width: 72px; height: 72px;
    background: rgba(16,185,129,0.1);
    border: 2px solid var(--success);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    font-size: 2rem;
  }
  .modal-title {
    font-family: 'Cinzel', serif;
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--gold);
    margin-bottom: 0.75rem;
  }
  .modal-text { color: var(--muted); font-size: 0.95rem; margin-bottom: 1.5rem; }
  .modal-id {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 0.75rem 1.25rem;
    font-family: monospace;
    font-size: 0.85rem;
    color: var(--gold2);
    margin-bottom: 1.5rem;
    word-break: break-all;
  }
  .btn-close-modal {
    background: linear-gradient(135deg, var(--gold), var(--amber));
    color: #000;
    font-weight: 700;
    padding: 0.7rem 2rem;
    border-radius: 8px;
    border: none;
    font-size: 0.9rem;
    font-family: 'Outfit', sans-serif;
    cursor: pointer;
    transition: all 0.2s;
  }
 
  /* ── TOAST ── */
  .toast {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1rem 1.5rem;
    font-size: 0.9rem;
    max-width: 340px;
    z-index: 300;
    transform: translateY(100px);
    opacity: 0;
    transition: all 0.35s cubic-bezier(0.34,1.56,0.64,1);
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }
  .toast.show { transform: translateY(0); opacity: 1; }
  .toast.error { border-color: rgba(239,68,68,0.4); }
  .toast.success { border-color: rgba(16,185,129,0.4); }
  .toast-icon { font-size: 1.2rem; flex-shrink: 0; }
 
  /* ── CONTACT ── */
  #contact {
    padding: 5rem 2rem;
    position: relative;
    z-index: 1;
  }
  .contact-inner {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
  }
  .contact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin-top: 2.5rem;
  }
  .contact-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 1.75rem 1rem;
    transition: border-color 0.3s, transform 0.3s;
  }
  .contact-card:hover { border-color: rgba(212,168,67,0.3); transform: translateY(-3px); }
  .contact-icon { font-size: 1.8rem; margin-bottom: 0.75rem; display: block; }
  .contact-label { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.08em; color: var(--muted); margin-bottom: 0.35rem; }
  .contact-val { font-weight: 500; color: var(--text); font-size: 0.95rem; }
 
  /* ── FOOTER ── */
  footer {
    border-top: 1px solid var(--border);
    padding: 2rem;
    text-align: center;
    color: var(--muted);
    font-size: 0.85rem;
    position: relative;
    z-index: 1;
  }
  footer span { color: var(--gold); }
 
  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  /* Ensure hero content always visible even if animation blocked */
  .hero-badge, .hero-title, .hero-sub, .hero-desc, .hero-divider, .hero-buttons, .hero-stats {
    opacity: 1 !important;
  }
 
  .fade-in {
    opacity: 1;
    transform: translateY(0);
    transition: opacity 0.6s ease, transform 0.6s ease;
  }
  .fade-in.animate { opacity: 0; transform: translateY(20px); }
  .fade-in.animate.visible { opacity: 1; transform: translateY(0); }
 
  /* ── LOADER OVERLAY ── */
  #loader {
    position: fixed; inset: 0;
    background: var(--bg);
    z-index: 999;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 1rem;
    transition: opacity 0.5s;
  }
  #loader.hidden { opacity: 0; pointer-events: none; }
  .loader-logo {
    font-family: 'Cinzel', serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--gold);
    letter-spacing: 0.1em;
  }
  .loader-bar {
    width: 120px; height: 2px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }
  .loader-bar::after {
    content: '';
    display: block;
    height: 100%;
    background: linear-gradient(90deg, var(--gold), var(--amber));
    animation: load 1.2s ease both;
  }
  @keyframes load { from { width: 0; } to { width: 100%; } }
 
  /* mobile nav */
  @media (max-width: 768px) {
    nav { padding: 1rem 1.5rem; }
    .nav-links { display: none; }
    .form-card { padding: 1.75rem 1.25rem; }
  }
 
  .firebase-note {
    background: rgba(212,168,67,0.05);
    border: 1px solid rgba(212,168,67,0.15);
    border-radius: 10px;
    padding: 1.25rem 1.5rem;
    margin-bottom: 2rem;
    font-size: 0.85rem;
    color: var(--muted);
    line-height: 1.7;
  }
  .firebase-note strong { color: var(--gold); }
  .firebase-note code {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 0.1rem 0.4rem;
    font-size: 0.82rem;
    color: var(--gold2);
  }
</style>
</head>
<body>
 
<!-- LOADER -->
<div id="loader" style="display:none;">
  <div class="loader-logo">✦ VEDA TECH EXPO</div>
  <div class="loader-bar"></div>
</div>
 
<!-- NAV -->
<nav>
  <div class="nav-left">
    <img src="[BASE64_IMAGE_DATA_EMBEDDED]" alt="KL IRD" class="nav-logo-img" id="klLogo"/>
  </div>
  <div class="nav-logo">✦ Veda Tech Expo</div>
  <div class="nav-right">
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#register">Register</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="#register" class="nav-cta">Register Now</a></li>
    </ul>
    <img src="[BASE64_IMAGE_DATA_EMBEDDED]" alt="VEDA Club" class="nav-logo-img" id="vedaLogo"/>
  </div>
</nav>
 
<!-- DEADLINE BANNER -->
<div style="background:linear-gradient(90deg,#92400e,#b45309,#92400e);text-align:center;padding:0.55rem 1rem;font-size:0.82rem;font-weight:600;letter-spacing:0.06em;color:#fef3c7;position:fixed;top:64px;left:0;right:0;z-index:99;">
  ⚡ Registration closes <strong>March 27</strong> &nbsp;·&nbsp; Expo on <strong>March 31</strong> at KLU, SAC Hall &nbsp;·&nbsp; Register now!
</div>
 
<!-- HERO -->
<section id="hero">
  <div class="hero-glow"></div>
  <div class="hero-badge">✦ 2026 Annual Event</div>
  <h1 class="hero-title"><span>Veda Tech</span><br/>Expo 2026</h1>
  <p class="hero-sub">Innovate · Collaborate · Inspire</p>
  <div class="hero-divider"></div>
  <p class="hero-desc">
    A premier technology exposition bringing together brilliant minds to showcase groundbreaking research and innovation.
  </p>
  <div class="hero-buttons">
    <a href="#register" class="btn-primary">Register Your Team</a>
    <a href="#about" class="btn-secondary">Learn More</a>
  </div>
  <div class="hero-stats">
    <div class="stat"><span class="stat-num">3–5</span><span class="stat-label">Members / Team</span></div>
    <div class="stat"><span class="stat-num">Mar 31</span><span class="stat-label">Expo Date</span></div>
    <div class="stat"><span class="stat-num">Mar 27</span><span class="stat-label">Registration Closes</span></div>
  </div>
</section>
 
<!-- ABOUT -->
<section id="about">
  <div class="section-header fade-in">
    <p class="section-tag">✦ About the Event</p>
    <h2 class="section-title">What is Veda Tech Expo?</h2>
    <div class="section-line"></div>
  </div>
  <div class="about-grid">
    <div class="about-card fade-in">
      <span class="card-icon">🔬</span>
      <div class="card-title">Research Showcase</div>
      <p class="card-text">Present your research work, innovations, and technological breakthroughs to a panel of industry experts and academicians.</p>
    </div>
    <div class="about-card fade-in">
      <span class="card-icon">👥</span>
      <div class="card-title">Team Based</div>
      <p class="card-text">Form teams of 3 to 5 members. Students from inside and outside the campus are welcome to participate and collaborate.</p>
    </div>
    <div class="about-card fade-in">
      <span class="card-icon">🏆</span>
      <div class="card-title">Win & Excel</div>
      <p class="card-text">Compete for recognition, prizes, and the opportunity to network with like-minded innovators and industry professionals.</p>
    </div>
    <div class="about-card fade-in">
      <span class="card-icon">📋</span>
      <div class="card-title">Easy Registration</div>
      <p class="card-text">Fill out the simple team registration form below. All member details are stored securely and you'll receive a confirmation ID.</p>
    </div>
  </div>
</section>
 
<!-- REGISTER -->
<section id="register">
  <div class="register-wrap">
    <div class="section-header fade-in">
      <p class="section-tag">✦ Team Registration</p>
      <h2 class="section-title">Register Your Team</h2>
      <div class="section-line"></div>
    </div>
 
    <!-- Firebase setup note (dev-visible) -->
    <div class="firebase-note fade-in" id="setupNote">
      ⚙️ <strong>Setup Required:</strong> Replace the Firebase config below with your own project credentials.
      Go to <strong>Firebase Console → Project Settings → Your Apps</strong> and copy the config object.
      Then set your Firestore Database rules to allow writes. This note will hide once Firebase is connected.
      <br/><br/>
      Collection name used: <code>registrations</code>
    </div>
 
    <div class="form-card fade-in">
      <form id="regForm" novalidate>
 
        <!-- TEAM INFO -->
        <div class="form-section">
          <div class="form-section-title">🏷️ Team Information</div>
          <div class="form-grid">
            <div class="field span2">
              <label>Team Name <span class="req">*</span></label>
              <input type="text" id="teamName" placeholder="e.g. Team Veda" required/>
            </div>
            <div class="field">
              <label>Project Title <span class="req">*</span></label>
              <input type="text" id="projectTitle" placeholder="Project / research title" required/>
            </div>
            <div class="field">
              <label>Domain / Category <span class="req">*</span></label>
              <select id="domain" required>
                <option value="">Select broad theme…</option>
                <option>Computing & Information Technology</option>
                <option>Electronics & Communication</option>
                <option>Mechanical & Manufacturing</option>
                <option>Civil & Structural Engineering</option>
                <option>Electrical & Power Systems</option>
                <option>Chemical & Biotechnology</option>
                <option>Environmental & Sustainable Tech</option>
                <option>Healthcare & Biomedical</option>
                <option>Agriculture & Food Technology</option>
                <option>Business & Management Innovation</option>
                <option>Design & Architecture</option>
                <option>Physics & Mathematics</option>
                <option>Social Sciences & Humanities</option>
                <option>Interdisciplinary / Other</option>
              </select>
            </div>
            <div class="field">
              <label>College / Institution <span class="req">*</span></label>
              <input type="text" id="college" placeholder="Your college name" required/>
            </div>
            <div class="field">
              <label>Department <span class="req">*</span></label>
              <input type="text" id="department" placeholder="e.g. CSE, ECE…" required/>
            </div>
          </div>
        </div>
 
        <!-- TEAM LEADER -->
        <div class="form-section">
          <div class="form-section-title">👑 Team Leader</div>
          <div class="form-grid">
            <div class="field">
              <label>Full Name <span class="req">*</span></label>
              <input type="text" id="leaderName" placeholder="Leader's full name" required/>
            </div>
            <div class="field">
              <label>Email <span class="req">*</span></label>
              <input type="email" id="leaderEmail" placeholder="leader@email.com" required/>
            </div>
            <div class="field">
              <label>Phone <span class="req">*</span></label>
              <input type="tel" id="leaderPhone" placeholder="+91 XXXXX XXXXX" required/>
            </div>
            <div class="field">
              <label>Year / Semester <span class="req">*</span></label>
              <select id="leaderYear" required>
                <option value="">Select…</option>
                <option>1st Year</option>
                <option>2nd Year</option>
                <option>3rd Year</option>
                <option>4th Year</option>
                <option>PG / Masters</option>
                <option>PhD</option>
              </select>
            </div>
          </div>
        </div>
 
        <!-- MEMBERS -->
        <div class="form-section">
          <div class="form-section-title">👥 Team Members <span style="font-size:0.75rem; color:var(--muted); font-weight:400; text-transform:none; letter-spacing:0;">(2–4 additional members)</span></div>
          <div class="members-container" id="membersContainer"></div>
          <button type="button" class="add-member-btn" id="addMemberBtn" onclick="addMember()">+ Add Member</button>
          <p style="margin-top:0.5rem; font-size:0.78rem; color:#4b5563;">Team size: 3–5 members total (including leader). Max 4 additional members.</p>
        </div>
 
        <!-- PAYMENT (Optional) -->
        <div class="form-section">
          <div class="form-section-title">💳 Payment Details <span style="font-size:0.75rem; color:var(--muted); font-weight:400; text-transform:none; letter-spacing:0;">(optional)</span></div>
          <div class="form-grid">
            <div class="field">
              <label>Transaction ID <span class="opt">(optional)</span></label>
              <input type="text" id="transactionId" placeholder="UPI / Bank Transaction ID"/>
            </div>
            <div class="field">
              <label>Payment Screenshot <span class="opt">(optional)</span></label>
              <input type="file" id="paymentImg" accept="image/*"/>
            </div>
          </div>
        </div>
 
        <div class="submit-wrap">
          <button type="submit" class="btn-submit" id="submitBtn">
            ✦ Submit Registration
          </button>
          <p style="margin-top:1rem; font-size:0.8rem; color:#4b5563;">By registering you agree to the event terms and conditions.</p>
        </div>
 
      </form>
    </div>
  </div>
</section>
 
<!-- CONTACT -->
<section id="contact">
  <div class="contact-inner">
    <div class="section-header fade-in">
      <p class="section-tag">✦ Get In Touch</p>
      <h2 class="section-title">Contact Us</h2>
      <div class="section-line"></div>
    </div>
    <div class="contact-grid">
      <div class="contact-card fade-in">
        <span class="contact-icon">📧</span>
        <div class="contact-label">Email</div>
        <div class="contact-val">veda@kluniversity.in</div>
      </div>
      <div class="contact-card fade-in">
        <span class="contact-icon">📞</span>
        <div class="contact-label">Phone</div>
        <div class="contact-val">+91 94404 94599</div>
      </div>
      <div class="contact-card fade-in">
        <span class="contact-icon">📍</span>
        <div class="contact-label">Venue</div>
        <div class="contact-val">KLU, SAC Hall</div>
      </div>
    </div>
  </div>
</section>
 
<!-- FOOTER -->
<footer>
  <p>© 2026 <span>Veda Tech Expo</span> · All Rights Reserved · Built with ♥ for Innovation</p>
</footer>
 
<!-- SUCCESS MODAL -->
<div class="modal-overlay" id="successModal">
  <div class="modal">
    <div class="modal-icon">✓</div>
    <div class="modal-title">Registration Successful!</div>
    <p class="modal-text">Your team has been registered for Veda Tech Expo 2026. Save your registration ID:</p>
    <div class="modal-id" id="regIdDisplay"></div>
    <button class="btn-close-modal" onclick="closeModal()">Done</button>
  </div>
</div>
 
<!-- TOAST -->
<div class="toast" id="toast">
  <span class="toast-icon" id="toastIcon"></span>
  <span id="toastMsg"></span>
</div>
 
<!-- ══════════════════════════════════════════════════════════════
     FIREBASE SDK  — Replace the firebaseConfig with YOUR values
════════════════════════════════════════════════════════════════ -->
<script>
  // Fallback: always hide loader within 2 seconds no matter what
  document.addEventListener("DOMContentLoaded", function() {
    setTimeout(function() {
      var l = document.getElementById("loader");
      if (l) { l.style.opacity = "0"; l.style.pointerEvents = "none"; setTimeout(function(){ l.style.display="none"; }, 500); }
    }, 800);
  });
</script>
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
  import { getFirestore, collection, addDoc, serverTimestamp }
    from "https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js";
  import { getStorage, ref, uploadBytes, getDownloadURL }
    from "https://www.gstatic.com/firebasejs/10.12.2/firebase-storage.js";
 
  /* ── ⚠️  REPLACE THIS WITH YOUR FIREBASE PROJECT CONFIG ── */
  const firebaseConfig = {
    apiKey:            "AIzaSyCj3yoH158F9ccKifk1YNWtWGR3-RMt4mM",
    authDomain:        "veda-tech-expo.firebaseapp.com",
    projectId:         "veda-tech-expo",
    storageBucket:     "veda-tech-expo.firebasestorage.app",
    messagingSenderId: "886687406511",
    appId:             "1:886687406511:web:4572649c3d7198145b375a",
    measurementId:     "G-0BT7JN17QK"
  };
  /* ─────────────────────────────────────────────────────────── */
 
  let app, db, storage, firebaseReady = false;
 
  try {
    app     = initializeApp(firebaseConfig);
    db      = getFirestore(app);
    storage = getStorage(app);
    if (!firebaseConfig.apiKey.startsWith("YOUR_") && firebaseConfig.projectId !== "YOUR_PROJECT_ID") {
      firebaseReady = true;
      document.getElementById("setupNote").style.display = "none";
    }
  } catch (e) {
    console.warn("Firebase init skipped:", e.message);
  }
 
  // ── Member management ──
  let memberCount = 0;
 
  window.addMember = function() {
    if (memberCount >= 4) return;
    memberCount++;
    const idx = memberCount;
    const div = document.createElement("div");
    div.className = "member-block";
    div.id = `member_${idx}`;
    div.innerHTML = `
      <div class="member-block-header">
        <span class="member-label">Member ${idx}</span>
        ${idx > 1 ? `<button type="button" class="remove-member" onclick="removeMember(${idx})">Remove</button>` : ''}
      </div>
      <div class="form-grid">
        <div class="field">
          <label>Full Name <span class="req">*</span></label>
          <input type="text" name="m_name_${idx}" placeholder="Member's name" required/>
        </div>
        <div class="field">
          <label>Email <span class="req">*</span></label>
          <input type="email" name="m_email_${idx}" placeholder="member@email.com" required/>
        </div>
        <div class="field">
          <label>Phone</label>
          <input type="tel" name="m_phone_${idx}" placeholder="+91 XXXXX XXXXX"/>
        </div>
        <div class="field">
          <label>Year / Semester</label>
          <select name="m_year_${idx}">
            <option value="">Select…</option>
            <option>1st Year</option><option>2nd Year</option>
            <option>3rd Year</option><option>4th Year</option>
            <option>PG / Masters</option><option>PhD</option>
          </select>
        </div>
      </div>`;
    document.getElementById("membersContainer").appendChild(div);
    document.getElementById("addMemberBtn").disabled = memberCount >= 4;
  };
 
  window.removeMember = function(idx) {
    const el = document.getElementById(`member_${idx}`);
    if (el) el.remove();
    memberCount--;
    document.getElementById("addMemberBtn").disabled = false;
  };
 
  // Add first member automatically
  window.addMember();
 
  // ── Helpers ──
  function showToast(msg, type = "success") {
    const t = document.getElementById("toast");
    document.getElementById("toastMsg").textContent = msg;
    document.getElementById("toastIcon").textContent = type === "success" ? "✓" : "✕";
    t.className = `toast ${type} show`;
    setTimeout(() => { t.className = `toast ${type}`; }, 4000);
  }
 
  function generateRegId() {
    return "VTE26-" + Math.random().toString(36).substr(2,4).toUpperCase() + "-" + Date.now().toString(36).toUpperCase().slice(-4);
  }
 
  window.closeModal = function() {
    document.getElementById("successModal").classList.remove("show");
    document.getElementById("regForm").reset();
    document.getElementById("membersContainer").innerHTML = "";
    memberCount = 0;
    window.addMember();
  };
 
  // ── Collect member data from DOM ──
  function collectMembers() {
    const members = [];
    document.querySelectorAll(".member-block").forEach(block => {
      const idx = block.id.split("_")[1];
      const n  = block.querySelector(`[name="m_name_${idx}"]`)?.value.trim();
      const e  = block.querySelector(`[name="m_email_${idx}"]`)?.value.trim();
      const ph = block.querySelector(`[name="m_phone_${idx}"]`)?.value.trim();
      const yr = block.querySelector(`[name="m_year_${idx}"]`)?.value;
      if (n) members.push({ name: n, email: e, phone: ph, year: yr });
    });
    return members;
  }
 
  // ── Form Submit ──
  document.getElementById("regForm").addEventListener("submit", async function(e) {
    e.preventDefault();
    const btn = document.getElementById("submitBtn");
 
    // Validate
    const required = ["teamName","projectTitle","domain","college","department","leaderName","leaderEmail","leaderPhone","leaderYear"];
    let valid = true;
    required.forEach(id => {
      const el = document.getElementById(id);
      if (!el.value.trim()) { el.style.borderColor = "var(--error)"; valid = false; }
      else el.style.borderColor = "";
    });
 
    const members = collectMembers();
    const totalSize = 1 + members.length; // leader + members
    if (totalSize < 3) {
      showToast("Please add at least 2 team members (total 3 min).", "error");
      return;
    }
    if (!valid) { showToast("Please fill all required fields.", "error"); return; }
 
    btn.disabled = true;
    btn.textContent = "Submitting…";
 
    const regId = generateRegId();
 
    // Build data object
    const data = {
      regId,
      teamName:     document.getElementById("teamName").value.trim(),
      projectTitle: document.getElementById("projectTitle").value.trim(),
      domain:       document.getElementById("domain").value,
      college:      document.getElementById("college").value.trim(),
      department:   document.getElementById("department").value.trim(),
      leader: {
        name:  document.getElementById("leaderName").value.trim(),
        email: document.getElementById("leaderEmail").value.trim(),
        phone: document.getElementById("leaderPhone").value.trim(),
        year:  document.getElementById("leaderYear").value,
      },
      members,
      transactionId: document.getElementById("transactionId").value.trim() || null,
      paymentImageUrl: null,
      registeredAt: new Date().toISOString(),
      totalMembers: totalSize,
    };
 
    try {
      // Upload payment image if provided
      const imgFile = document.getElementById("paymentImg").files[0];
      if (imgFile && firebaseReady) {
        const imgRef = ref(storage, `payment_screenshots/${regId}_${imgFile.name}`);
        const snap   = await uploadBytes(imgRef, imgFile);
        data.paymentImageUrl = await getDownloadURL(snap.ref);
      }
 
      if (firebaseReady) {
        await addDoc(collection(db, "registrations"), { ...data, serverTimestamp: serverTimestamp() });
      } else {
        // Demo mode — just show success
        console.log("Demo mode — data:", data);
        await new Promise(r => setTimeout(r, 900));
      }
 
      document.getElementById("regIdDisplay").textContent = regId;
      document.getElementById("successModal").classList.add("show");
 
    } catch (err) {
      console.error(err);
      showToast("Submission failed. Please try again.", "error");
    } finally {
      btn.disabled = false;
      btn.textContent = "✦ Submit Registration";
    }
  });
 
  // ── Loader ──
  function hideLoader() {
    const l = document.getElementById("loader");
    if (l) { l.classList.add("hidden"); setTimeout(() => { l.style.display = "none"; }, 600); }
  }
  setTimeout(hideLoader, 1500);
  window.addEventListener("load", () => setTimeout(hideLoader, 300));
 
  // ── Scroll animations ──
  const observer = new IntersectionObser
